{% extends 'base1.html.twig' %}

{% block title %}Commentaires{% endblock %}

{% block body %}
    <h1>Commentaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ commentaire.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ commentaire.nom }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ commentaire.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaires_index') }}">Retour à la liste</a>

    <a href="{{ path('commentaires_edit', {'id': commentaire.id}) }}">Modifier</a>

    {{ include('commentaires/_delete_form.html.twig') }}
{% endblock %}
